package com.attendance.userservice.error;

public enum ErrorCode {
    VALIDATION_ERROR,
    NOT_FOUND,
    CONFLICT,
    UNAUTHORIZED,
    FORBIDDEN,
    TOKEN_INVALID,
    TOKEN_EXPIRED,
    INTERNAL_ERROR
}
